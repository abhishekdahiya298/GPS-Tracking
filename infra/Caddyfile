# Production reverse proxy in front of the web app only.
# Traccar's device port (5027) is never proxied here — it takes raw TCP from
# cellular devices and is exposed directly on the VPS, not through Caddy/Cloudflare.
{$RIO_DOMAIN} {
	reverse_proxy web:3000
}

# Traccar's own web UI, kept off the public domain — access via SSH tunnel or VPN
# instead of exposing an admin panel to the internet.
