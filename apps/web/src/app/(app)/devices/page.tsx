// Interim: devices are managed on the combined Vehicles & devices screen until
// the dedicated device table lands (UI phase E).
export { default, dynamic } from "../vehicles/page";
export const metadata = { title: "Devices · RIO GPS" };
